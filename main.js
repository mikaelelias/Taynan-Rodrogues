function scrollToNextSection() {
    const nextSection = document.querySelector('.container');
    
    window.scrollBy({
        top: 920, // Descer 100px
        behavior: 'smooth' // Suavizar a transição
    });
}

function scrollNext() {
    
}